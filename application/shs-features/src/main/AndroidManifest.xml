<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Wi-Fi file transfer server -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- QR scanner -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Vault biometric unlock -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />

    <!-- Vault file operations -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.MANAGE_MEDIA" />

    <application>
        <!-- SHS Features Activity — hosts Compose UI for Live TV, Vault, Transfer, QR -->
        <activity
            android:name="dev.shsplayer.features.ShsFeaturesActivity"
            android:exported="false"
            android:theme="@style/Theme.Material3.DayNight.NoActionBar" />

        <!-- Wi-Fi Transfer foreground service -->
        <service
            android:name="dev.shsplayer.features.wifiTransfer.WifiTransferService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!--
            FileProvider: VLC's own FileProvider (${applicationId}.provider) is
            used for vault restore and Wi-Fi transfer. We don't declare a
            separate one here to avoid manifest merger conflicts.
            VaultManager.getPlayableUri() uses "${packageName}.provider" as the
            authority to match VLC's existing FileProvider.
        -->
    </application>
</manifest>
